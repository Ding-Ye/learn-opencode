# Skill workspace — must NEVER be pushed (contains the upstream clone, ~30-50MB)
.learn/

# Go
*.exe
*.test
*.out
**/agents/*/coverage.txt
**/agents/*/main
# Per-session built binaries: each module compiles to a binary named after its dir,
# e.g. agents/s04-permission-eval/s04-permission-eval. Glob below catches all.
agents/*/s[0-9][0-9]-*/s[0-9][0-9]-*
agents/s[0-9][0-9]-*/s[0-9][0-9]-*

# Node / Next
node_modules/
.next/
out/
dist/
build/
*.log

# Editors / OS
.DS_Store
.idea/
.vscode/

# Env
.env
.env.local
